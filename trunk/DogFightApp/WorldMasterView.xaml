<UserControl x:Class="DogFightApp.WorldMasterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
  <GroupBox Header="WorldMasterView">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>
      <ListBox Grid.Row="0" 
               SelectedItem="{Binding SelectedFighter}" 
               ItemsSource="{Binding Fighters}" DisplayMemberPath="Name" >
      </ListBox>
      <WrapPanel Grid.Row="1" Orientation="Horizontal">
        <Button Content="Add" Command="{Binding AddFighterCommand}"/>
        <Button Content="Remove"/>
      </WrapPanel>
    </Grid>
  </GroupBox>
</UserControl>
